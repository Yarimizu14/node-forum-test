<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
        <% for(var i=0, len=list.length; i<len; i++) { %>
            <li>
            <div><a href="/forum/<%= list[i].title %>"><%= list[i].title %></a></div>
                <div><%= list[i].content %></div>
            </li>
        <% } %>
    </ul>
  </body>
</html>
